- defaults:
    link:
      relink: true

- clean: ['~']

- brewfile: [Brewfile]

- link:
    ~/.dotfiles: ''
    ~/.zshrc: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]

